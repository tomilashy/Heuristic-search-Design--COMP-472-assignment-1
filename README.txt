
Libraries used
shapefile (pip install pyshp)
matplotlib (pip install matplotlib)
numpy as np (pip install numpy)


#instructions
You just need to install the libraries, and add the folder called Shape to the same location as the python file.
the shape folder contains the crime_dt.shp file and other files given by the prof(crime_dt files). 
Then run the code
The 1 and 1.3 weighted lines were commented out in the code at line 322 and 326 because it made the graph kind of messy. 
so you could uncomment them if you want to see them in the graph